ifdef(`__BASE_SOLT__',`',
define(`__BASE_SOLT__')dnl
define(`IFVALUETYPE',`dnl
ifelse(
        regexp($1,bool),         0, $2,
        regexp($1,u?int\(\d*\)), 0, $2,
        regexp($1,u?fixed),      0, $2,
        regexp($1,address),      0, $2,
        regexp($1,bytes\([0-9]\)), 0, $2,
        regexp($1,bytes\([0-9][0-9]\)), 0, $2,
        $3)`'dnl
')dnl
)dnl
